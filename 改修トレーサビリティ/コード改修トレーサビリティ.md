# コード改修トレーサビリティ - Microsoft Graph SDK統合

## 改修概要

**改修日時**: 2025年6月24日  
**改修者**: Devin AI  
**改修内容**: transcript_minutes_api2にMicrosoft Graph SDK統合を追加  
**ブランチ**: main-tokunou-20250623-001  
**コミットハッシュ**: 23abe32  

## 改修目的

Teams会議のトランスクリプトをMicrosoft Graph APIから直接取得し、既存の議事録生成機能と統合することで、手動でのトランスクリプト入力を不要にする。

## 影響範囲

### 新規追加ファイル

#### 1. app/modules/graph_client.py
**目的**: Microsoft Graph APIクライアントの実装

```python
# 新規作成ファイル - 修正前: 存在しない

# 修正後: 完全新規実装
from msgraph import GraphServiceClient
from msgraph.generated.models.o_data_errors.o_data_error import ODataError
from azure.identity import ClientSecretCredential
from typing import List, Optional
import os
from dotenv import load_dotenv
from app.modules.logger import logger

load_dotenv()

TENANT_ID = os.getenv("GRAPH_TENANT_ID")
CLIENT_ID = os.getenv("GRAPH_CLIENT_ID") 
CLIENT_SECRET = os.getenv("GRAPH_CLIENT_SECRET")

class GraphClient:
    def __init__(self):
        if not all([TENANT_ID, CLIENT_ID, CLIENT_SECRET]):
            logger.warning("Graph API credentials not fully configured")
            self.client = None
            return
            
        try:
            credential = ClientSecretCredential(
                tenant_id=TENANT_ID,
                client_id=CLIENT_ID,
                client_secret=CLIENT_SECRET
            )
            
            self.client = GraphServiceClient(
                credentials=credential,
                scopes=['https://graph.microsoft.com/.default']
            )
            logger.info("Graph client initialized successfully")
            
        except Exception as e:
            logger.error(f"Failed to initialize Graph client: {str(e)}")
            self.client = None
    
    async def get_user_meetings(self, user_id: str) -> List[dict]:
        # 会議一覧取得メソッド実装
        
    async def get_meeting_transcripts(self, user_id: str, meeting_id: str) -> List[dict]:
        # トランスクリプト一覧取得メソッド実装
        
    async def get_transcript_content(self, user_id: str, meeting_id: str, transcript_id: str) -> str:
        # トランスクリプト内容取得メソッド実装

graph_client = GraphClient()
```

#### 2. app/schemas/graph.py
**目的**: Microsoft Graph API用のPydanticスキーマ定義

```python
# 新規作成ファイル - 修正前: 存在しない

# 修正後: 完全新規実装
from pydantic import BaseModel, Field
from typing import List, Optional

class MeetingInfo(BaseModel):
    id: str
    subject: Optional[str]
    start_time: Optional[str]
    end_time: Optional[str]
    join_url: Optional[str]

class MeetingListResponse(BaseModel):
    meetings: List[MeetingInfo]
    
class TranscriptInfo(BaseModel):
    id: str
    created_date_time: Optional[str]
    end_date_time: Optional[str]
    content_url: Optional[str]

class TranscriptListResponse(BaseModel):
    transcripts: List[TranscriptInfo]

class TranscriptContentResponse(BaseModel):
    transcript_id: str
    content: str
    
class GraphMinutesGenerate(BaseModel):
    user_id: str = Field(..., description="Microsoft Graph user ID")
    meeting_id: str = Field(..., description="Online meeting ID")
    transcript_id: str = Field(..., description="Transcript ID")
    title: Optional[str] = Field(None, max_length=200, description="Optional meeting title")
```

#### 3. app/routers/graph.py
**目的**: Microsoft Graph API用のFastAPIルーター

```python
# 新規作成ファイル - 修正前: 存在しない

# 修正後: 完全新規実装
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from typing import List
from app.modules.database import get_db, User, Minutes
from app.modules.auth_handler import get_current_user
from app.modules.graph_client import graph_client
from app.modules.openai_client import generate_minutes
from app.schemas.graph import (
    MeetingListResponse, 
    TranscriptListResponse, 
    TranscriptContentResponse,
    GraphMinutesGenerate
)
from app.schemas.minutes import MinutesResponse
from app.modules.logger import logger, log_api_call
import time

router = APIRouter(prefix="/graph", tags=["microsoft graph"])

@router.get("/meetings/{user_id}", response_model=MeetingListResponse)
async def get_user_meetings(user_id: str, current_user: User = Depends(get_current_user)):
    # ユーザー会議一覧取得エンドポイント実装

@router.get("/meetings/{user_id}/{meeting_id}/transcripts", response_model=TranscriptListResponse)
async def get_meeting_transcripts(user_id: str, meeting_id: str, current_user: User = Depends(get_current_user)):
    # 会議トランスクリプト一覧取得エンドポイント実装

@router.get("/meetings/{user_id}/{meeting_id}/transcripts/{transcript_id}/content", response_model=TranscriptContentResponse)
async def get_transcript_content(user_id: str, meeting_id: str, transcript_id: str, current_user: User = Depends(get_current_user)):
    # トランスクリプト内容取得エンドポイント実装

@router.post("/minutes/generate", response_model=MinutesResponse, status_code=status.HTTP_201_CREATED)
async def generate_minutes_from_graph(request: GraphMinutesGenerate, current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    # Graph APIから議事録生成エンドポイント実装
```

### 既存ファイル修正

#### 1. pyproject.toml
**目的**: Microsoft Graph SDK依存関係の追加

```toml
# 修正前
[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.104.0"
uvicorn = "^0.24.0"
pydantic = {extras = ["email"], version = "^2.5.0"}
python-jose = "^3.3.0"
passlib = "^1.7.4"
bcrypt = "^4.1.0"
openai = "^1.3.0"
python-dotenv = "^1.0.0"
sqlalchemy = "^2.0.0"
python-multipart = "^0.0.6"

# 修正後
[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.104.0"
uvicorn = "^0.24.0"
pydantic = {extras = ["email"], version = "^2.5.0"}
python-jose = "^3.3.0"
passlib = "^1.7.4"
bcrypt = "^4.1.0"
openai = "^1.3.0"
python-dotenv = "^1.0.0"
sqlalchemy = "^2.0.0"
python-multipart = "^0.0.6"
msgraph-sdk = "^1.34.0"        # 追加
azure-identity = "^1.15.0"     # 追加
```

#### 2. app/main.py
**目的**: Graph APIルーターの追加

```python
# 修正前
from app.routers import auth, users, minutes

app.include_router(auth.router)
app.include_router(users.router)
app.include_router(minutes.router)

# 修正後
from app.routers import auth, users, minutes, graph

app.include_router(auth.router)
app.include_router(users.router)
app.include_router(minutes.router)
app.include_router(graph.router)  # 追加
```

#### 3. .env
**目的**: Microsoft Graph API設定の追加

```env
# 修正前
# データベース設定
DATABASE_URL=sqlite:///./app.db

# JWT設定
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OpenAI設定
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-3.5-turbo

# ログ設定
LOG_LEVEL=INFO
LOG_FILE=app.log

# CORS設定
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# 修正後
# データベース設定
DATABASE_URL=sqlite:///./app.db

# JWT設定
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OpenAI設定
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-3.5-turbo

# ログ設定
LOG_LEVEL=INFO
LOG_FILE=app.log

# CORS設定
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Microsoft Graph Configuration  # 追加
GRAPH_TENANT_ID=your_tenant_id_here
GRAPH_CLIENT_ID=your_client_id_here
GRAPH_CLIENT_SECRET=your_client_secret_here
```

#### 4. README.md
**目的**: Graph API使用例とセットアップ手順の追加

```markdown
# 修正前
### 3. 議事録生成

```bash
curl -X POST "http://localhost:8000/minutes/generate" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "transcript": "今日の会議では新しいプロジェクトについて話し合いました...",
    "title": "プロジェクト企画会議"
  }'
```

# 修正後
### 3. 議事録生成（手動入力）

```bash
curl -X POST "http://localhost:8000/minutes/generate" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "transcript": "今日の会議では新しいプロジェクトについて話し合いました...",
    "title": "プロジェクト企画会議"
  }'
```

### 4. Microsoft Graph API使用例  # 追加セクション

#### 4.1 ユーザーの会議一覧取得

```bash
curl -X GET "http://localhost:8000/graph/meetings/{user_id}" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### 4.2 会議のトランスクリプト一覧取得

```bash
curl -X GET "http://localhost:8000/graph/meetings/{user_id}/{meeting_id}/transcripts" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### 4.3 トランスクリプト内容取得

```bash
curl -X GET "http://localhost:8000/graph/meetings/{user_id}/{meeting_id}/transcripts/{transcript_id}/content" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### 4.4 Graph APIから議事録生成

```bash
curl -X POST "http://localhost:8000/graph/minutes/generate" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "user_id": "graph_user_id",
    "meeting_id": "meeting_id",
    "transcript_id": "transcript_id",
    "title": "Teams会議議事録"
  }'
```
```

## 新規追加エンドポイント

### Graph API関連エンドポイント

| メソッド | エンドポイント | 説明 | 認証 |
|---------|---------------|------|------|
| GET | `/graph/meetings/{user_id}` | ユーザーの会議一覧取得 | JWT必須 |
| GET | `/graph/meetings/{user_id}/{meeting_id}/transcripts` | 会議のトランスクリプト一覧取得 | JWT必須 |
| GET | `/graph/meetings/{user_id}/{meeting_id}/transcripts/{transcript_id}/content` | トランスクリプト内容取得 | JWT必須 |
| POST | `/graph/minutes/generate` | Graph APIから議事録生成 | JWT必須 |

## 技術的詳細

### 依存関係追加
- `msgraph-sdk = "^1.34.0"`: Microsoft Graph SDK for Python
- `azure-identity = "^1.15.0"`: Azure認証ライブラリ

### 認証方式
- **ClientSecretCredential**: アプリケーション権限での認証
- **必要な権限**:
  - `OnlineMeetingTranscript.Read.All`
  - `OnlineMeeting.Read.All`

### エラーハンドリング
- Graph API未設定時の適切なエラーレスポンス
- ODataError例外の適切な処理
- 既存機能への影響なし

## テスト結果

### インポートテスト
```bash
✅ poetry run python -c "from app.modules.graph_client import graph_client; print('Graph client imported successfully')"
✅ poetry run python -c "from app.routers.graph import router; print('Graph router imported successfully')"
✅ poetry run python -c "from app.schemas.graph import MeetingInfo, GraphMinutesGenerate; print('Graph schemas imported successfully')"
```

### 依存関係インストール
```bash
✅ poetry lock --no-update
✅ poetry install
```

## 後方互換性

- 既存のAPIエンドポイントは変更なし
- 既存の手動トランスクリプト入力機能は維持
- Graph API設定が未完了でも既存機能は正常動作

## セキュリティ考慮事項

- Graph API認証情報は環境変数で管理
- JWT認証による適切なアクセス制御
- Graph API未設定時の適切なエラーハンドリング

## 今後の拡張可能性

- 他のMicrosoft 365サービスとの統合
- リアルタイムトランスクリプト取得
- 会議録画の自動処理
- Teams Botとの統合

## 改修完了確認

- [x] Microsoft Graph SDK依存関係追加
- [x] GraphClientクラス実装
- [x] Graph API用スキーマ定義
- [x] Graph APIルーター実装
- [x] メインアプリケーションへの統合
- [x] 環境変数設定追加
- [x] README更新
- [x] テスト実行確認
- [x] コミット・プッシュ完了
- [x] PR更新完了

**改修ステータス**: ✅ 完了  
**PR URL**: https://github.com/EM-Dev-Git/devin_dit_demo/pull/1
