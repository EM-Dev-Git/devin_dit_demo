# 結合テスト設計書 - transcript_minutes_api2

## 概要

**対象システム**: transcript_minutes_api2  
**作成日**: 2025年6月24日  
**テストフレームワーク**: pytest + FastAPI TestClient + 実データベース  
**テスト対象**: コンポーネント間連携、エンドツーエンドワークフロー、外部API統合

## テスト戦略

### 基本方針
- **コンポーネント統合**: 認証、データベース、OpenAI、Graph APIモジュール間の連携テスト
- **エンドツーエンドワークフロー**: ユーザー登録から議事録生成までの完全な業務フローテスト
- **実データベース**: SQLiteを使用したトランザクション管理とデータ整合性テスト
- **外部API統合**: OpenAI APIとMicrosoft Graph SDKの実際の統合テスト

### 単体テストとの差別化
| 観点 | 単体テスト | 結合テスト |
|------|-----------|-----------|
| データベース | インメモリSQLite | 実SQLiteファイル |
| 外部API | 完全モック | 選択的実API呼び出し |
| テスト範囲 | 個別コンポーネント | コンポーネント間連携 |
| テスト目的 | 機能検証 | 統合・ワークフロー検証 |

## テスト環境設定

### 基本設定
```python
# tests/integration/conftest.py
import pytest
import os
import tempfile
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.modules.database import Base, get_db

@pytest.fixture(scope="session")
def integration_db():
    """実データベースファイルを使用した統合テスト用DB"""
    db_fd, db_path = tempfile.mkstemp(suffix='.db')
    engine = create_engine(f"sqlite:///{db_path}")
    Base.metadata.create_all(bind=engine)
    
    SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
    
    yield SessionLocal, engine
    
    os.close(db_fd)
    os.unlink(db_path)

@pytest.fixture
def integration_client(integration_db):
    """統合テスト用クライアント"""
    SessionLocal, engine = integration_db
    
    def override_get_db():
        db = SessionLocal()
        try:
            yield db
        finally:
            db.close()
    
    app.dependency_overrides[get_db] = override_get_db
    
    with TestClient(app) as client:
        yield client
    
    app.dependency_overrides.clear()
```

## 1. コンポーネント統合テスト

### 1.1 認証 + データベース統合テスト

#### テスト対象
- JWT認証とデータベースユーザー検索の連携
- トークン検証とユーザー情報取得の一貫性
- 認証エラー時のデータベース状態

#### 実装例
```python
class TestAuthDatabaseIntegration:
    def test_jwt_token_database_user_lookup_flow(self, integration_client):
        """JWT認証とDB検索の完全フロー"""
        # 1. ユーザー登録
        register_data = {
            "username": "integrationuser",
            "email": "integration@test.com",
            "password": "securepass123"
        }
        register_response = integration_client.post("/auth/register", json=register_data)
        assert register_response.status_code == 201
        
        # 2. ログインしてトークン取得
        login_data = {"username": "integrationuser", "password": "securepass123"}
        login_response = integration_client.post("/auth/login", json=login_data)
        assert login_response.status_code == 200
        token = login_response.json()["access_token"]
        
        # 3. 認証が必要なエンドポイントでトークン検証
        headers = {"Authorization": f"Bearer {token}"}
        profile_response = integration_client.get("/users/profile", headers=headers)
        assert profile_response.status_code == 200
        assert profile_response.json()["username"] == "integrationuser"
    
    def test_token_expiry_database_consistency(self, integration_client):
        """トークン期限切れ時のDB状態確認"""
        # 期限切れトークンでのアクセス試行
        # データベース内のユーザー情報が変更されていないことを確認
    
    def test_concurrent_user_authentication(self, integration_client):
        """複数ユーザーの同時認証処理"""
        # 複数ユーザーの並行登録・ログイン
        # データベースの整合性確認
```

### 1.2 OpenAI + データベース統合テスト

#### テスト対象
- 議事録生成とデータベース保存の一貫性
- OpenAI API呼び出し失敗時のデータベース状態
- トランザクション管理とロールバック

#### 実装例
```python
class TestOpenAIDatabaseIntegration:
    def test_minutes_generation_database_persistence_flow(self, integration_client, auth_headers):
        """議事録生成からDB保存までの完全フロー"""
        # 1. 議事録生成リクエスト
        transcript_data = {
            "transcript": "これはテスト用の会議トランスクリプトです。",
            "title": "統合テスト会議"
        }
        
        # 2. OpenAI API呼び出し + DB保存
        response = integration_client.post(
            "/minutes/generate", 
            json=transcript_data, 
            headers=auth_headers
        )
        assert response.status_code == 201
        minutes_id = response.json()["id"]
        
        # 3. 保存された議事録の取得確認
        get_response = integration_client.get(
            f"/minutes/{minutes_id}", 
            headers=auth_headers
        )
        assert get_response.status_code == 200
        assert get_response.json()["title"] == "統合テスト会議"
    
    @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OpenAI API key not available")
    def test_real_openai_api_integration(self, integration_client, auth_headers):
        """実際のOpenAI APIとの統合テスト"""
        # 実際のOpenAI API呼び出しを含む統合テスト
        # レート制限とエラーハンドリングの確認
    
    def test_openai_failure_database_rollback(self, integration_client, auth_headers, mocker):
        """OpenAI失敗時のデータベースロールバック"""
        # OpenAI API失敗をシミュレート
        mocker.patch('app.modules.openai_client.client.chat.completions.create', 
                    side_effect=Exception("API Error"))
        
        transcript_data = {"transcript": "テストトランスクリプト"}
        response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
        
        # エラーレスポンス確認
        assert response.status_code == 500
        
        # データベースに不完全なレコードが残っていないことを確認
        history_response = integration_client.get("/minutes/history", headers=auth_headers)
        assert len(history_response.json()["minutes"]) == 0
```

### 1.3 Graph API + OpenAI統合テスト

#### テスト対象
- Teams会議トランスクリプト取得から議事録生成までのフロー
- Graph API認証とOpenAI API呼び出しの連携
- 複数の外部API間のエラーハンドリング

#### 実装例
```python
class TestGraphOpenAIIntegration:
    def test_teams_transcript_to_minutes_complete_flow(self, integration_client, auth_headers, mock_graph_client):
        """Teams → Graph API → OpenAI → DB の完全フロー"""
        # 1. Graph APIからトランスクリプト取得をモック
        mock_graph_client.get_transcript_content.return_value = "会議の内容です。"
        
        # 2. Graph経由での議事録生成
        graph_data = {
            "user_id": "test-user-id",
            "meeting_id": "test-meeting-id", 
            "transcript_id": "test-transcript-id",
            "title": "Teams会議"
        }
        
        response = integration_client.post(
            "/graph/generate-minutes", 
            json=graph_data, 
            headers=auth_headers
        )
        assert response.status_code == 201
        
        # 3. 生成された議事録の確認
        minutes_id = response.json()["id"]
        get_response = integration_client.get(f"/minutes/{minutes_id}", headers=auth_headers)
        assert get_response.status_code == 200
    
    def test_graph_api_failure_error_propagation(self, integration_client, auth_headers, mock_graph_client):
        """Graph API失敗時のエラー伝播"""
        # Graph API失敗をシミュレート
        mock_graph_client.get_transcript_content.side_effect = Exception("Graph API Error")
        
        graph_data = {
            "user_id": "test-user-id",
            "meeting_id": "test-meeting-id",
            "transcript_id": "test-transcript-id"
        }
        
        response = integration_client.post("/graph/generate-minutes", json=graph_data, headers=auth_headers)
        assert response.status_code == 500
        assert "Graph API Error" in response.json()["detail"]
```

### 1.4 ログ統合テスト

#### テスト対象
- 全コンポーネント横断でのログ出力
- エラー発生時のログトレーサビリティ
- パフォーマンスログの一貫性

#### 実装例
```python
class TestLoggingIntegration:
    def test_cross_component_logging_flow(self, integration_client, auth_headers, caplog):
        """コンポーネント横断ログフロー"""
        with caplog.at_level(logging.INFO):
            # 議事録生成の完全フロー実行
            transcript_data = {"transcript": "ログテスト用トランスクリプト"}
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
            
            # 各コンポーネントのログ出力確認
            log_messages = [record.message for record in caplog.records]
            assert any("Generating meeting minutes" in msg for msg in log_messages)
            assert any("API Call - Endpoint: /minutes/generate" in msg for msg in log_messages)
            assert any("OpenAI Call - User:" in msg for msg in log_messages)
    
    def test_error_logging_traceability(self, integration_client, auth_headers, caplog, mocker):
        """エラー時のログトレーサビリティ"""
        # OpenAI API失敗をシミュレート
        mocker.patch('app.modules.openai_client.client.chat.completions.create', 
                    side_effect=Exception("Test Error"))
        
        with caplog.at_level(logging.ERROR):
            transcript_data = {"transcript": "エラーテスト"}
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
            
            # エラーログの確認
            error_logs = [record.message for record in caplog.records if record.levelname == "ERROR"]
            assert any("OpenAI API error" in msg for msg in error_logs)
            assert any("Failed to generate minutes" in msg for msg in error_logs)
```

## 2. エンドツーエンドワークフローテスト

### 2.1 完全ユーザージャーニーテスト

#### テスト対象
- ユーザー登録 → ログイン → 議事録生成 → 履歴確認の完全フロー
- プロフィール更新とセッション管理
- 複数議事録の管理と検索

#### 実装例
```python
class TestCompleteUserJourney:
    def test_complete_user_lifecycle(self, integration_client):
        """ユーザーライフサイクル完全テスト"""
        # 1. ユーザー登録
        register_data = {
            "username": "journeyuser",
            "email": "journey@test.com", 
            "password": "journey123"
        }
        register_response = integration_client.post("/auth/register", json=register_data)
        assert register_response.status_code == 201
        
        # 2. ログイン
        login_data = {"username": "journeyuser", "password": "journey123"}
        login_response = integration_client.post("/auth/login", json=login_data)
        assert login_response.status_code == 200
        token = login_response.json()["access_token"]
        headers = {"Authorization": f"Bearer {token}"}
        
        # 3. プロフィール確認
        profile_response = integration_client.get("/users/profile", headers=headers)
        assert profile_response.status_code == 200
        assert profile_response.json()["username"] == "journeyuser"
        
        # 4. 複数の議事録生成
        minutes_ids = []
        for i in range(3):
            transcript_data = {
                "transcript": f"会議{i+1}のトランスクリプト内容",
                "title": f"会議{i+1}"
            }
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
            assert response.status_code == 201
            minutes_ids.append(response.json()["id"])
        
        # 5. 履歴確認
        history_response = integration_client.get("/minutes/history", headers=headers)
        assert history_response.status_code == 200
        assert len(history_response.json()["minutes"]) == 3
        
        # 6. 個別議事録取得
        for minutes_id in minutes_ids:
            get_response = integration_client.get(f"/minutes/{minutes_id}", headers=headers)
            assert get_response.status_code == 200
        
        # 7. プロフィール更新
        update_data = {"email": "updated@test.com"}
        update_response = integration_client.put("/users/profile", json=update_data, headers=headers)
        assert update_response.status_code == 200
        assert update_response.json()["email"] == "updated@test.com"
    
    def test_multi_user_isolation(self, integration_client):
        """複数ユーザーのデータ分離テスト"""
        # 2人のユーザーを作成
        users = []
        for i in range(2):
            # ユーザー登録・ログイン
            register_data = {
                "username": f"user{i}",
                "email": f"user{i}@test.com",
                "password": "password123"
            }
            integration_client.post("/auth/register", json=register_data)
            
            login_data = {"username": f"user{i}", "password": "password123"}
            login_response = integration_client.post("/auth/login", json=login_data)
            token = login_response.json()["access_token"]
            users.append({"token": token, "username": f"user{i}"})
        
        # 各ユーザーが議事録を生成
        for i, user in enumerate(users):
            headers = {"Authorization": f"Bearer {user['token']}"}
            transcript_data = {
                "transcript": f"{user['username']}の会議内容",
                "title": f"{user['username']}の会議"
            }
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
            assert response.status_code == 201
        
        # 各ユーザーが自分の議事録のみ見えることを確認
        for user in users:
            headers = {"Authorization": f"Bearer {user['token']}"}
            history_response = integration_client.get("/minutes/history", headers=headers)
            assert history_response.status_code == 200
            minutes_list = history_response.json()["minutes"]
            assert len(minutes_list) == 1
            assert user["username"] in minutes_list[0]["title"]
```

### 2.2 Graph API統合ワークフローテスト

#### テスト対象
- Teams会議一覧取得 → トランスクリプト一覧 → 内容取得 → 議事録生成
- Graph API認証フローの統合
- 大量データ処理のパフォーマンス

#### 実装例
```python
class TestGraphAPIWorkflow:
    def test_teams_meeting_complete_workflow(self, integration_client, auth_headers, mock_graph_client):
        """Teams会議処理の完全ワークフロー"""
        # 1. ユーザーの会議一覧取得
        mock_graph_client.get_user_meetings.return_value = [
            {
                "id": "meeting1",
                "subject": "週次ミーティング",
                "start_time": "2025-06-24T10:00:00Z",
                "end_time": "2025-06-24T11:00:00Z"
            }
        ]
        
        meetings_response = integration_client.get("/graph/meetings/test-user-id", headers=auth_headers)
        assert meetings_response.status_code == 200
        meetings = meetings_response.json()["meetings"]
        assert len(meetings) == 1
        
        # 2. 特定会議のトランスクリプト一覧取得
        mock_graph_client.get_meeting_transcripts.return_value = [
            {
                "id": "transcript1",
                "created_date_time": "2025-06-24T10:00:00Z",
                "end_date_time": "2025-06-24T11:00:00Z"
            }
        ]
        
        transcripts_response = integration_client.get(
            "/graph/meetings/test-user-id/meeting1/transcripts", 
            headers=auth_headers
        )
        assert transcripts_response.status_code == 200
        transcripts = transcripts_response.json()["transcripts"]
        assert len(transcripts) == 1
        
        # 3. トランスクリプト内容取得
        mock_graph_client.get_transcript_content.return_value = "詳細な会議内容のトランスクリプト"
        
        content_response = integration_client.get(
            "/graph/meetings/test-user-id/meeting1/transcripts/transcript1/content",
            headers=auth_headers
        )
        assert content_response.status_code == 200
        assert "詳細な会議内容" in content_response.json()["content"]
        
        # 4. 取得したトランスクリプトから議事録生成
        generate_data = {
            "user_id": "test-user-id",
            "meeting_id": "meeting1",
            "transcript_id": "transcript1",
            "title": "週次ミーティング議事録"
        }
        
        minutes_response = integration_client.post(
            "/graph/generate-minutes",
            json=generate_data,
            headers=auth_headers
        )
        assert minutes_response.status_code == 201
        
        # 5. 生成された議事録の確認
        minutes_id = minutes_response.json()["id"]
        final_response = integration_client.get(f"/minutes/{minutes_id}", headers=auth_headers)
        assert final_response.status_code == 200
        assert final_response.json()["title"] == "週次ミーティング議事録"
```

## 3. データベーストランザクション統合テスト

### 3.1 トランザクション管理テスト

#### テスト対象
- 複数テーブル間のトランザクション整合性
- エラー時のロールバック動作
- 同時実行制御

#### 実装例
```python
class TestDatabaseTransactionIntegration:
    def test_user_minutes_transaction_consistency(self, integration_client):
        """ユーザーと議事録のトランザクション整合性"""
        # ユーザー作成とログイン
        register_data = {"username": "txuser", "email": "tx@test.com", "password": "tx123"}
        integration_client.post("/auth/register", json=register_data)
        
        login_data = {"username": "txuser", "password": "tx123"}
        login_response = integration_client.post("/auth/login", json=login_data)
        headers = {"Authorization": f"Bearer {login_response.json()['access_token']}"}
        
        # 議事録生成（成功ケース）
        transcript_data = {"transcript": "トランザクションテスト", "title": "TX会議"}
        response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
        assert response.status_code == 201
        
        # データベース内の関連性確認
        history_response = integration_client.get("/minutes/history", headers=headers)
        assert len(history_response.json()["minutes"]) == 1
    
    def test_concurrent_minutes_generation(self, integration_client):
        """同時議事録生成のトランザクション制御"""
        import threading
        import time
        
        # 複数スレッドでの同時議事録生成
        # データベースロックとトランザクション分離の確認
        results = []
        
        def generate_minutes(thread_id):
            # 各スレッドでユーザー作成・ログイン・議事録生成
            register_data = {
                "username": f"concurrent{thread_id}",
                "email": f"concurrent{thread_id}@test.com",
                "password": "concurrent123"
            }
            integration_client.post("/auth/register", json=register_data)
            
            login_data = {"username": f"concurrent{thread_id}", "password": "concurrent123"}
            login_response = integration_client.post("/auth/login", json=login_data)
            headers = {"Authorization": f"Bearer {login_response.json()['access_token']}"}
            
            transcript_data = {
                "transcript": f"スレッド{thread_id}のトランスクリプト",
                "title": f"スレッド{thread_id}会議"
            }
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
            results.append((thread_id, response.status_code))
        
        # 5つのスレッドで同時実行
        threads = []
        for i in range(5):
            thread = threading.Thread(target=generate_minutes, args=(i,))
            threads.append(thread)
            thread.start()
        
        for thread in threads:
            thread.join()
        
        # 全てのリクエストが成功していることを確認
        assert len(results) == 5
        assert all(status == 201 for _, status in results)
```

### 3.2 データ整合性テスト

#### テスト対象
- 外部キー制約の動作
- カスケード削除の動作
- データ型制約の検証

#### 実装例
```python
class TestDataIntegrityIntegration:
    def test_foreign_key_constraints(self, integration_client, integration_db):
        """外部キー制約の統合テスト"""
        SessionLocal, engine = integration_db
        
        # ユーザー作成
        register_data = {"username": "fkuser", "email": "fk@test.com", "password": "fk123"}
        integration_client.post("/auth/register", json=register_data)
        
        login_data = {"username": "fkuser", "password": "fk123"}
        login_response = integration_client.post("/auth/login", json=login_data)
        headers = {"Authorization": f"Bearer {login_response.json()['access_token']}"}
        
        # 議事録作成
        transcript_data = {"transcript": "FK制約テスト", "title": "FK会議"}
        minutes_response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
        minutes_id = minutes_response.json()["id"]
        
        # データベース直接操作でユーザー削除を試行（外部キー制約で失敗するはず）
        with SessionLocal() as db:
            from app.modules.database import User, Minutes
            user = db.query(User).filter(User.username == "fkuser").first()
            
            # ユーザーに紐づく議事録が存在する状態でユーザー削除を試行
            try:
                db.delete(user)
                db.commit()
                assert False, "外部キー制約が働いていない"
            except Exception:
                db.rollback()
                # 外部キー制約が正常に動作
                pass
        
        # 議事録が正常に取得できることを確認
        get_response = integration_client.get(f"/minutes/{minutes_id}", headers=headers)
        assert get_response.status_code == 200
```

## 4. 外部API統合テスト

### 4.1 OpenAI API統合テスト

#### テスト対象
- 実際のOpenAI API呼び出し
- レート制限とエラーハンドリング
- 大量データ処理のパフォーマンス

#### 実装例
```python
class TestOpenAPIIntegration:
    @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OpenAI API key not configured")
    def test_real_openai_api_call(self, integration_client, auth_headers):
        """実際のOpenAI APIとの統合テスト"""
        transcript_data = {
            "transcript": """
            会議参加者: 田中、佐藤、鈴木
            議題: 来月のプロジェクト計画について
            田中: 来月のスケジュールを確認しましょう。
            佐藤: 開発フェーズは2週間を予定しています。
            鈴木: テストフェーズも含めて3週間必要です。
            田中: 了解しました。リソース配分を検討します。
            """,
            "title": "プロジェクト計画会議"
        }
        
        response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
        assert response.status_code == 201
        
        # 生成された議事録の品質確認
        minutes_data = response.json()
        assert "田中" in minutes_data["generated_minutes"]
        assert "プロジェクト" in minutes_data["generated_minutes"]
        assert minutes_data["title"] == "プロジェクト計画会議"
    
    @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OpenAI API key not configured")
    def test_openai_rate_limiting(self, integration_client, auth_headers):
        """OpenAI APIレート制限テスト"""
        # 短時間で複数リクエストを送信
        responses = []
        for i in range(5):
            transcript_data = {
                "transcript": f"レート制限テスト{i}の内容",
                "title": f"テスト会議{i}"
            }
            response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
            responses.append(response)
            time.sleep(1)  # 1秒間隔
        
        # 全てのリクエストが適切に処理されることを確認
        success_count = sum(1 for r in responses if r.status_code == 201)
        assert success_count >= 3  # 最低3つは成功するはず
```

### 4.2 Microsoft Graph SDK統合テスト

#### テスト対象
- Graph SDK初期化と認証
- Teams API呼び出しのエラーハンドリング
- 大量会議データの処理

#### 実装例
```python
class TestGraphSDKIntegration:
    @pytest.mark.skipif(
        not all([os.getenv("GRAPH_TENANT_ID"), os.getenv("GRAPH_CLIENT_ID"), os.getenv("GRAPH_CLIENT_SECRET")]),
        reason="Graph API credentials not configured"
    )
    def test_graph_sdk_initialization(self, integration_client):
        """Graph SDK初期化テスト"""
        from app.modules.graph_client import graph_client
        
        # Graph クライアントが正常に初期化されていることを確認
        assert graph_client.client is not None
    
    def test_graph_api_error_handling(self, integration_client, auth_headers, mock_graph_client):
        """Graph API エラーハンドリング統合テスト"""
        # 様々なGraph APIエラーをシミュレート
        error_scenarios = [
            ("Unauthorized", "認証エラー"),
            ("NotFound", "リソースが見つからない"),
            ("TooManyRequests", "レート制限"),
            ("InternalServerError", "サーバーエラー")
        ]
        
        for error_type, description in error_scenarios:
            mock_graph_client.get_user_meetings.side_effect = Exception(f"Graph API Error: {error_type}")
            
            response = integration_client.get("/graph/meetings/test-user-id", headers=auth_headers)
            assert response.status_code == 500
            assert error_type in response.json()["detail"]
```

## 5. セキュリティ統合テスト

### 5.1 認証・認可統合テスト

#### テスト対象
- JWT トークンライフサイクル
- 権限境界の確認
- セッション管理

#### 実装例
```python
class TestSecurityIntegration:
    def test_jwt_token_lifecycle_integration(self, integration_client):
        """JWT トークンライフサイクル統合テスト"""
        # ユーザー登録・ログイン
        register_data = {"username": "secuser", "email": "sec@test.com", "password": "sec123"}
        integration_client.post("/auth/register", json=register_data)
        
        login_data = {"username": "secuser", "password": "sec123"}
        login_response = integration_client.post("/auth/login", json=login_data)
        token = login_response.json()["access_token"]
        headers = {"Authorization": f"Bearer {token}"}
        
        # トークンを使用した認証済みアクセス
        profile_response = integration_client.get("/users/profile", headers=headers)
        assert profile_response.status_code == 200
        
        # トークンリフレッシュ
        refresh_response = integration_client.post("/auth/refresh", headers=headers)
        assert refresh_response.status_code == 200
        new_token = refresh_response.json()["access_token"]
        
        # 新しいトークンでのアクセス
        new_headers = {"Authorization": f"Bearer {new_token}"}
        profile_response2 = integration_client.get("/users/profile", headers=new_headers)
        assert profile_response2.status_code == 200
    
    def test_cross_user_data_isolation(self, integration_client):
        """ユーザー間データ分離テスト"""
        # 2人のユーザーを作成
        users = []
        for i in range(2):
            register_data = {
                "username": f"isolationuser{i}",
                "email": f"isolation{i}@test.com",
                "password": "isolation123"
            }
            integration_client.post("/auth/register", json=register_data)
            
            login_data = {"username": f"isolationuser{i}", "password": "isolation123"}
            login_response = integration_client.post("/auth/login", json=login_data)
            token = login_response.json()["access_token"]
            users.append({"token": token, "id": i})
        
        # ユーザー1が議事録を作成
        headers1 = {"Authorization": f"Bearer {users[0]['token']}"}
        transcript_data = {"transcript": "ユーザー1の秘密会議", "title": "機密会議"}
        response1 = integration_client.post("/minutes/generate", json=transcript_data, headers=headers1)
        minutes_id = response1.json()["id"]
        
        # ユーザー2が他ユーザーの議事録にアクセス試行
        headers2 = {"Authorization": f"Bearer {users[1]['token']}"}
        response2 = integration_client.get(f"/minutes/{minutes_id}", headers=headers2)
        assert response2.status_code == 404  # アクセス拒否
        
        # ユーザー2の履歴には表示されない
        history_response = integration_client.get("/minutes/history", headers=headers2)
        assert len(history_response.json()["minutes"]) == 0
```

## 6. パフォーマンス統合テスト

### 6.1 負荷テスト

#### テスト対象
- 同時ユーザー処理
- 大量データ処理
- メモリ使用量

#### 実装例
```python
class TestPerformanceIntegration:
    def test_concurrent_user_load(self, integration_client):
        """同時ユーザー負荷テスト"""
        import threading
        import time
        
        results = []
        start_time = time.time()
        
        def user_workflow(user_id):
            try:
                # ユーザー登録
                register_data = {
                    "username": f"loaduser{user_id}",
                    "email": f"load{user_id}@test.com",
                    "password": "load123"
                }
                register_response = integration_client.post("/auth/register", json=register_data)
                
                # ログイン
                login_data = {"username": f"loaduser{user_id}", "password": "load123"}
                login_response = integration_client.post("/auth/login", json=login_data)
                token = login_response.json()["access_token"]
                headers = {"Authorization": f"Bearer {token}"}
                
                # 議事録生成
                transcript_data = {
                    "transcript": f"ユーザー{user_id}の負荷テスト会議内容",
                    "title": f"負荷テスト会議{user_id}"
                }
                minutes_response = integration_client.post("/minutes/generate", json=transcript_data, headers=headers)
                
                results.append({
                    "user_id": user_id,
                    "success": minutes_response.status_code == 201,
                    "response_time": time.time() - start_time
                })
            except Exception as e:
                results.append({
                    "user_id": user_id,
                    "success": False,
                    "error": str(e)
                })
        
        # 10ユーザーの同時実行
        threads = []
        for i in range(10):
            thread = threading.Thread(target=user_workflow, args=(i,))
            threads.append(thread)
            thread.start()
        
        for thread in threads:
            thread.join()
        
        # 結果分析
        success_rate = sum(1 for r in results if r["success"]) / len(results)
        avg_response_time = sum(r.get("response_time", 0) for r in results if r["success"]) / len([r for r in results if r["success"]])
        
        assert success_rate >= 0.8  # 80%以上の成功率
        assert avg_response_time < 30.0  # 平均30秒以内
    
    def test_large_transcript_processing(self, integration_client, auth_headers):
        """大量トランスクリプト処理テスト"""
        # 大きなトランスクリプトデータを生成
        large_transcript = "会議内容: " + "テストデータ " * 1000  # 約10KB
        
        transcript_data = {
            "transcript": large_transcript,
            "title": "大量データテスト会議"
        }
        
        start_time = time.time()
        response = integration_client.post("/minutes/generate", json=transcript_data, headers=auth_headers)
        processing_time = time.time() - start_time
        
        assert response.status_code == 201
        assert processing_time < 60.0  # 60秒以内で処理完了
        
        # 生成された議事録のサイズ確認
        minutes_data = response.json()
        assert len(minutes_data["generated_minutes"]) > 0
```

## 7. テスト実行戦略

### 7.1 テスト分類

#### 高速統合テスト (Fast Integration Tests)
- データベース統合テスト
- コンポーネント間連携テスト
- モック化された外部API統合テスト
- 実行時間: 各テスト < 5秒

#### 低速統合テスト (Slow Integration Tests)
- 実際の外部API呼び出しテスト
- パフォーマンステスト
- 大量データ処理テスト
- 実行時間: 各テスト < 60秒

### 7.2 実行コマンド

```bash
# 高速統合テストのみ実行
pytest tests/integration/ -m "not slow" -v

# 低速統合テストのみ実行
pytest tests/integration/ -m "slow" -v

# 全統合テスト実行
pytest tests/integration/ -v

# カバレッジ付き実行
pytest tests/integration/ --cov=app --cov-report=html -v
```

### 7.3 CI/CD統合

#### GitHub Actions設定例
```yaml
name: Integration Tests
on: [push, pull_request]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.11
      
      - name: Install dependencies
        run: |
          cd transcript_minutes_api2
          pip install poetry
          poetry install
      
      - name: Run fast integration tests
        run: |
          cd transcript_minutes_api2
          poetry run pytest tests/integration/ -m "not slow" -v
      
      - name: Run slow integration tests
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
          GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
          GRAPH_CLIENT_SECRET: ${{ secrets.GRAPH_CLIENT_SECRET }}
        run: |
          cd transcript_minutes_api2
          poetry run pytest tests/integration/ -m "slow" -v
```

## 8. 期待される成果

### 8.1 品質向上
- コンポーネント間の連携不具合の早期発見
- 実際の運用環境に近い条件でのテスト
- データ整合性とトランザクション安全性の確保

### 8.2 信頼性向上
- 外部API障害時の適切なエラーハンドリング確認
- 同時ユーザーアクセス時の安定性確認
- セキュリティ境界の適切な実装確認

### 8.3 保守性向上
- システム変更時の影響範囲の明確化
- リグレッションテストの自動化
- 運用監視ポイントの特定

## 9. 今後の拡張計画

### 9.1 追加テストシナリオ
- モバイルアプリケーション統合テスト
- 多言語対応統合テスト
- 障害復旧シナリオテスト

### 9.2 テスト環境改善
- Dockerコンテナ化によるテスト環境統一
- テストデータ管理の自動化
- パフォーマンス監視の強化

### 9.3 継続的改善
- テスト結果の分析とメトリクス収集
- テストケースの定期的な見直し
- 新機能追加時のテスト戦略策定

---

**文書作成者**: Devin AI  
**最終更新日**: 2025年6月24日  
**承認者**: [承認者名]  
**次回レビュー予定**: 2025年7月24日
