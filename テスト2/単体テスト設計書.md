# 単体テスト設計書

## 1. 概要

### 1.1 目的
本文書は、transcript_minutes_api システムの単体テストの設計と実装方針を定義する。各コンポーネントの独立したテストを通じて、システムの品質と信頼性を確保することを目的とする。

### 1.2 対象システム
- **システム名**: transcript_minutes_api
- **フレームワーク**: FastAPI
- **言語**: Python 3.12+
- **主要依存関係**: SQLAlchemy, OpenAI API, JWT認証

### 1.3 テスト方針
- **テストフレームワーク**: pytest
- **カバレッジ目標**: 90%以上
- **テスト分類**: 単体テスト（コンポーネント独立テスト）
- **モック戦略**: 外部依存関係の完全モック化

## 2. テスト環境設定

### 2.1 必要パッケージ
```toml
[tool.poetry.group.test.dependencies]
pytest = "^7.4.0"
pytest-asyncio = "^0.21.0"
pytest-cov = "^4.1.0"
pytest-mock = "^3.11.0"
httpx = "^0.24.0"
```

### 2.2 pytest設定
```ini
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = "--cov=app --cov-report=html --cov-report=term-missing"
```

### 2.3 テストディレクトリ構造
```
tests/
├── __init__.py
├── conftest.py
├── unit/
│   ├── __init__.py
│   ├── routers/
│   │   ├── test_auth.py
│   │   ├── test_minutes.py
│   │   └── test_users.py
│   ├── modules/
│   │   ├── test_auth_handler.py
│   │   ├── test_database.py
│   │   ├── test_openai_client.py
│   │   └── test_logger.py
│   └── schemas/
│       ├── test_auth_schemas.py
│       ├── test_minutes_schemas.py
│       └── test_users_schemas.py
└── fixtures/
    ├── auth_fixtures.py
    ├── database_fixtures.py
    └── openai_fixtures.py
```

## 3. APIエンドポイントテスト設計

### 3.1 認証ルーター（/auth）テスト

#### 3.1.1 POST /auth/register
**テスト対象**: `app.routers.auth.register_user`

**成功シナリオ**:
- 有効なユーザーデータでの登録成功
- レスポンス形式の検証（UserResponse）
- データベースへの正しい保存確認

**エラーシナリオ**:
- 重複ユーザー名での登録失敗（400エラー）
- 重複メールアドレスでの登録失敗（400エラー）
- 無効なメールアドレス形式（422エラー）
- データベースエラー時の処理（500エラー）

**テスト実装例**:
```python
def test_register_user_success(client, mock_db):
    user_data = {
        "username": "testuser",
        "email": "test@example.com",
        "password": "securepass123"
    }
    response = client.post("/auth/register", json=user_data)
    assert response.status_code == 201
    assert response.json()["username"] == "testuser"
    assert "id" in response.json()
```

#### 3.1.2 POST /auth/login
**テスト対象**: `app.routers.auth.login_user`

**成功シナリオ**:
- 正しい認証情報でのログイン成功
- JWTトークンの生成確認
- トークン形式の検証

**エラーシナリオ**:
- 存在しないユーザーでのログイン失敗（401エラー）
- 間違ったパスワードでのログイン失敗（401エラー）
- 必須フィールド不足（422エラー）

#### 3.1.3 POST /auth/refresh
**テスト対象**: `app.routers.auth.refresh_token`

**成功シナリオ**:
- 有効なトークンでのリフレッシュ成功
- 新しいトークンの生成確認

**エラーシナリオ**:
- 無効なトークンでのリフレッシュ失敗（401エラー）
- 期限切れトークンでのリフレッシュ失敗（401エラー）

### 3.2 議事録ルーター（/minutes）テスト

#### 3.2.1 POST /minutes/generate
**テスト対象**: `app.routers.minutes.generate_minutes`

**成功シナリオ**:
- 有効なトランスクリプトでの議事録生成成功
- OpenAI APIレスポンスの正しい処理
- データベースへの保存確認
- 認証ユーザーとの関連付け確認

**エラーシナリオ**:
- 未認証ユーザーのアクセス拒否（401エラー）
- OpenAI APIエラー時の処理（500エラー）
- 無効なリクエストデータ（422エラー）

**モック設定**:
```python
@pytest.fixture
def mock_openai_success(mocker):
    mock_response = mocker.MagicMock()
    mock_response.choices[0].message.content = "生成された議事録"
    return mocker.patch(
        'app.modules.openai_client.client.chat.completions.create',
        return_value=mock_response
    )
```

#### 3.2.2 GET /minutes/history
**テスト対象**: `app.routers.minutes.get_minutes_history`

**成功シナリオ**:
- 認証ユーザーの議事録履歴取得成功
- ページネーション機能の確認
- ユーザー固有データの分離確認

**エラーシナリオ**:
- 未認証ユーザーのアクセス拒否（401エラー）
- 無効なページネーションパラメータ

#### 3.2.3 GET /minutes/{minutes_id}
**テスト対象**: `app.routers.minutes.get_minutes_by_id`

**成功シナリオ**:
- 存在する議事録の取得成功
- ユーザー所有権の確認

**エラーシナリオ**:
- 存在しない議事録ID（404エラー）
- 他ユーザーの議事録アクセス拒否（404エラー）
- 未認証ユーザーのアクセス拒否（401エラー）

### 3.3 ユーザールーター（/users）テスト

#### 3.3.1 GET /users/profile
**テスト対象**: `app.routers.users.get_user_profile`

**成功シナリオ**:
- 認証ユーザーのプロファイル取得成功
- レスポンス形式の確認（UserProfile）

**エラーシナリオ**:
- 未認証ユーザーのアクセス拒否（401エラー）

#### 3.3.2 PUT /users/profile
**テスト対象**: `app.routers.users.update_user_profile`

**成功シナリオ**:
- ユーザー名の更新成功
- メールアドレスの更新成功
- 部分更新の確認

**エラーシナリオ**:
- 重複ユーザー名での更新失敗（400エラー）
- 重複メールアドレスでの更新失敗（400エラー）
- データベースエラー時の処理（500エラー）

## 4. モジュールテスト設計

### 4.1 認証ハンドラー（auth_handler.py）テスト

#### 4.1.1 パスワード関連関数
**テスト対象**: `verify_password`, `get_password_hash`

**テストケース**:
- パスワードハッシュ化の確認
- ハッシュ化されたパスワードの検証成功
- 間違ったパスワードの検証失敗
- 空文字列パスワードの処理

```python
def test_password_hashing():
    password = "testpassword123"
    hashed = get_password_hash(password)
    assert verify_password(password, hashed) is True
    assert verify_password("wrongpassword", hashed) is False
```

#### 4.1.2 JWT関連関数
**テスト対象**: `create_access_token`, `verify_token`

**テストケース**:
- JWTトークン生成の確認
- 有効なトークンの検証成功
- 無効なトークンの検証失敗
- 期限切れトークンの検証失敗
- カスタム有効期限の設定確認

#### 4.1.3 ユーザー操作関数
**テスト対象**: `authenticate_user`, `create_user`, `get_user_by_username`, `get_user_by_email`

**テストケース**:
- ユーザー認証の成功・失敗
- ユーザー作成の成功
- ユーザー検索の成功・失敗
- データベーストランザクションの確認

#### 4.1.4 現在ユーザー取得
**テスト対象**: `get_current_user`

**テストケース**:
- 有効なトークンでのユーザー取得成功
- 無効なトークンでの例外発生
- 存在しないユーザーでの例外発生

### 4.2 OpenAIクライアント（openai_client.py）テスト

#### 4.2.1 議事録生成関数
**テスト対象**: `generate_meeting_minutes`

**成功シナリオ**:
- 有効なトランスクリプトでの議事録生成成功
- タイトル付きトランスクリプトの処理
- OpenAI APIレスポンスの正しい解析

**エラーシナリオ**:
- APIキー未設定時のエラー処理
- OpenAI APIエラー時の例外処理
- 空のトランスクリプト処理

**モック実装**:
```python
@pytest.fixture
def mock_openai_client(mocker):
    mock_client = mocker.MagicMock()
    mock_response = mocker.MagicMock()
    mock_response.choices[0].message.content = "テスト議事録"
    mock_client.chat.completions.create.return_value = mock_response
    return mocker.patch('app.modules.openai_client.client', mock_client)
```

### 4.3 データベース（database.py）テスト

#### 4.3.1 モデルテスト
**テスト対象**: `User`, `Minutes`モデル

**テストケース**:
- モデルインスタンスの作成確認
- フィールド制約の確認
- リレーションシップの確認
- デフォルト値の設定確認

#### 4.3.2 データベース接続
**テスト対象**: `get_db`, `create_tables`

**テストケース**:
- データベース接続の確認
- テーブル作成の確認
- セッション管理の確認

### 4.4 ロガー（logger.py）テスト

#### 4.4.1 ログ設定
**テスト対象**: `setup_logger`, `get_logger`

**テストケース**:
- ログレベル設定の確認
- ログファイル出力の確認
- コンソール出力の確認
- 環境変数による設定変更の確認

## 5. スキーマテスト設計

### 5.1 認証スキーマ（auth.py）テスト

#### 5.1.1 UserCreateスキーマ
**テストケース**:
- 有効なデータでの検証成功
- 無効なメールアドレス形式での検証失敗
- 必須フィールド不足での検証失敗
- パスワード長制限の確認

#### 5.1.2 UserResponseスキーマ
**テストケース**:
- データベースモデルからの変換確認
- 日付フィールドの文字列変換確認
- 必須フィールドの存在確認

#### 5.1.3 Tokenスキーマ
**テストケース**:
- トークンタイプの確認
- アクセストークンの形式確認

### 5.2 議事録スキーマ（minutes.py）テスト

#### 5.2.1 MinutesGenerateスキーマ
**テストケース**:
- 必須フィールド（transcript）の確認
- オプショナルフィールド（title）の確認
- 空文字列の処理確認

#### 5.2.2 MinutesResponseスキーマ
**テストケース**:
- データベースモデルからの変換確認
- 日付フィールドの処理確認
- 全フィールドの存在確認

### 5.3 ユーザースキーマ（users.py）テスト

#### 5.3.1 UserProfileUpdateスキーマ
**テストケース**:
- 部分更新データの処理確認
- オプショナルフィールドの確認
- 無効なメールアドレス形式での検証失敗

## 6. テストユーティリティとフィクスチャ

### 6.1 共通フィクスチャ（conftest.py）

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.modules.database import Base, get_db

@pytest.fixture
def test_db():
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(bind=engine)
    TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
    
    def override_get_db():
        db = TestingSessionLocal()
        try:
            yield db
        finally:
            db.close()
    
    app.dependency_overrides[get_db] = override_get_db
    yield TestingSessionLocal
    app.dependency_overrides.clear()

@pytest.fixture
def client(test_db):
    return TestClient(app)

@pytest.fixture
def test_user(test_db):
    from app.modules.auth_handler import create_user
    db = test_db()
    user = create_user(db, "testuser", "test@example.com", "password123")
    db.close()
    return user

@pytest.fixture
def auth_headers(test_user):
    from app.modules.auth_handler import create_access_token
    token = create_access_token(data={"sub": test_user.username})
    return {"Authorization": f"Bearer {token}"}
```

### 6.2 OpenAIモックフィクスチャ

```python
@pytest.fixture
def mock_openai_success(mocker):
    mock_response = mocker.MagicMock()
    mock_response.choices[0].message.content = "生成された議事録内容"
    return mocker.patch(
        'app.modules.openai_client.client.chat.completions.create',
        return_value=mock_response
    )

@pytest.fixture
def mock_openai_error(mocker):
    return mocker.patch(
        'app.modules.openai_client.client.chat.completions.create',
        side_effect=Exception("OpenAI API Error")
    )
```

### 6.3 環境変数モック

```python
@pytest.fixture
def mock_env_vars(mocker):
    env_vars = {
        "SECRET_KEY": "test-secret-key",
        "OPENAI_API_KEY": "test-openai-key",
        "DATABASE_URL": "sqlite:///:memory:"
    }
    for key, value in env_vars.items():
        mocker.patch.dict(os.environ, {key: value})
```

## 7. エラーハンドリングテスト

### 7.1 HTTPException処理
- 各エンドポイントでの適切なHTTPステータスコード確認
- エラーメッセージの内容確認
- エラーレスポンス形式の確認

### 7.2 外部API障害対応
- OpenAI APIタイムアウト時の処理
- OpenAI API認証エラー時の処理
- OpenAI APIレート制限時の処理

### 7.3 データベース障害対応
- データベース接続エラー時の処理
- トランザクションロールバックの確認
- 制約違反エラーの処理

## 8. パフォーマンステスト

### 8.1 レスポンス時間測定
```python
import time

def test_endpoint_response_time(client, auth_headers):
    start_time = time.time()
    response = client.get("/users/profile", headers=auth_headers)
    end_time = time.time()
    
    assert response.status_code == 200
    assert (end_time - start_time) < 1.0  # 1秒以内
```

### 8.2 メモリ使用量監視
- 大量データ処理時のメモリリーク確認
- ガベージコレクション動作確認

## 9. テスト実行とカバレッジ

### 9.1 テスト実行コマンド
```bash
# 全テスト実行
pytest

# カバレッジ付き実行
pytest --cov=app --cov-report=html

# 特定モジュールのテスト
pytest tests/unit/modules/test_auth_handler.py

# 並列実行
pytest -n auto
```

### 9.2 カバレッジ目標
- **全体カバレッジ**: 90%以上
- **各モジュール**: 85%以上
- **クリティカルパス**: 100%

### 9.3 カバレッジ除外項目
- 設定ファイル読み込み部分
- ログ出力部分
- 例外処理の一部（到達困難なケース）

## 10. CI/CD統合

### 10.1 GitHub Actions設定例
```yaml
name: Unit Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install poetry
          poetry install
      - name: Run tests
        run: poetry run pytest --cov=app --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v3
```

### 10.2 品質ゲート
- テスト成功率: 100%
- カバレッジ: 90%以上
- 実行時間: 5分以内

## 11. テストデータ管理

### 11.1 テストデータ戦略
- **ファクトリーパターン**: 動的テストデータ生成
- **フィクスチャ**: 再利用可能な固定データ
- **モック**: 外部依存関係の代替

### 11.2 テストデータクリーンアップ
- 各テスト後のデータベースクリーンアップ
- 一時ファイルの削除
- モック状態のリセット

## 12. 保守性とスケーラビリティ

### 12.1 テストコードの保守性
- DRY原則の適用
- 明確なテスト名の付与
- 適切なコメントの追加
- テストヘルパー関数の活用

### 12.2 新機能追加時の考慮事項
- 既存テストへの影響評価
- 新規テストケースの追加
- カバレッジ維持の確認

## 13. まとめ

本設計書に基づいて単体テストを実装することで、transcript_minutes_apiシステムの品質と信頼性を確保できる。定期的なテスト実行とカバレッジ監視により、継続的な品質改善を図ることが重要である。

### 13.1 実装優先度
1. **高優先度**: 認証関連、議事録生成機能
2. **中優先度**: ユーザー管理、データベース操作
3. **低優先度**: ログ機能、設定管理

### 13.2 期待効果
- バグの早期発見と修正
- リファクタリング時の安全性確保
- 新機能追加時の既存機能への影響確認
- コードの可読性と保守性向上

---

**文書作成日**: 2025年6月24日  
**作成者**: Devin AI  
**対象システム**: transcript_minutes_api v1.0  
**次回更新予定**: 実装完了後
