# 単体テスト設計書 - transcript_minutes_api2

## 概要

**対象システム**: transcript_minutes_api2  
**作成日**: 2025年6月24日  
**テストフレームワーク**: pytest + FastAPI TestClient  
**テスト対象**: 全12エンドポイント、6モジュール、全スキーマ

## テスト戦略

### 基本方針
- **AAA パターン**: Arrange-Act-Assert構造
- **依存性注入オーバーライド**: 外部API（OpenAI、Graph）のモック化
- **インメモリSQLite**: 独立したテスト環境
- **包括的エラーシナリオ**: 正常系・異常系の両方をカバー

### テスト環境設定

```python
# tests/conftest.py - 基本設定
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.modules.database import Base, get_db

SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(SQLALCHEMY_DATABASE_URL, connect_args={"check_same_thread": False})
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

@pytest.fixture
def client(db_session):
    def override_get_db():
        yield db_session
    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as test_client:
        yield test_client
    app.dependency_overrides.clear()
```

## APIエンドポイントテスト設計

### 1. 認証エンドポイント (/auth) - 3エンドポイント

#### テストケース一覧
| エンドポイント | 正常系 | 異常系 | 認証 |
|---------------|--------|--------|------|
| POST /auth/register | ユーザー登録成功 | 重複ユーザー名/メール、無効データ | 不要 |
| POST /auth/login | ログイン成功 | 無効認証情報 | 不要 |
| POST /auth/refresh | トークンリフレッシュ成功 | 無効トークン | 必要 |

#### 実装例
```python
class TestAuthRegister:
    def test_register_success(self, client):
        user_data = {"username": "testuser", "email": "test@example.com", "password": "securepassword123"}
        response = client.post("/auth/register", json=user_data)
        assert response.status_code == 201
        assert response.json()["username"] == "testuser"
    
    def test_register_duplicate_username(self, client):
        # 事前ユーザー作成 → 重複登録試行 → 400エラー確認
    
    def test_register_invalid_data(self, client):
        # 短いユーザー名、無効メール、短いパスワード → 422エラー確認
```

### 2. ユーザーエンドポイント (/users) - 2エンドポイント

#### テストケース一覧
| エンドポイント | 正常系 | 異常系 | 認証 |
|---------------|--------|--------|------|
| GET /users/profile | プロフィール取得成功 | 未認証アクセス | 必要 |
| PUT /users/profile | プロフィール更新成功 | 重複データ、未認証 | 必要 |

### 3. 議事録エンドポイント (/minutes) - 3エンドポイント

#### テストケース一覧
| エンドポイント | 正常系 | 異常系 | 外部API |
|---------------|--------|--------|---------|
| POST /minutes/generate | 議事録生成成功 | OpenAIエラー、無効データ | OpenAI |
| GET /minutes/history | 履歴取得成功、ページネーション | 未認証 | なし |
| GET /minutes/{id} | 特定議事録取得成功 | 存在しないID、他ユーザー | なし |

#### OpenAI APIモック例
```python
@pytest.fixture
def mock_openai_response(self, mocker):
    mock_response = mocker.MagicMock()
    mock_response.choices[0].message.content = "生成された議事録内容"
    mock_response.usage.total_tokens = 150
    return mock_response

def test_generate_minutes_success(self, client, auth_headers, mocker, mock_openai_response):
    mocker.patch('app.modules.openai_client.client.chat.completions.create', return_value=mock_openai_response)
    # テスト実行
```

### 4. Microsoft Graph エンドポイント (/graph) - 4エンドポイント

#### テストケース一覧
| エンドポイント | 正常系 | 異常系 | 外部API |
|---------------|--------|--------|---------|
| GET /graph/meetings/{user_id} | 会議一覧取得成功 | Graph未設定、APIエラー | Graph |
| GET /graph/meetings/{user_id}/{meeting_id}/transcripts | トランスクリプト一覧取得 | Graph未設定 | Graph |
| GET /graph/meetings/{user_id}/{meeting_id}/transcripts/{transcript_id}/content | 内容取得成功 | 存在しないID | Graph |
| POST /graph/minutes/generate | Graph→議事録生成成功 | トランスクリプト未発見 | Graph+OpenAI |

#### Graph APIモック例
```python
@pytest.fixture
def mock_graph_client(self, mocker):
    mock_client = mocker.patch('app.modules.graph_client.graph_client')
    mock_client.get_user_meetings.return_value = [
        {"id": "meeting1", "subject": "テスト会議1", "start_time": "2025-06-24T10:00:00Z"}
    ]
    return mock_client
```

## モジュール単体テスト設計

### 1. 認証ハンドラー (auth_handler.py) - 6関数

#### テスト対象関数
- `verify_password()`: パスワード検証成功/失敗
- `get_password_hash()`: パスワードハッシュ化
- `create_access_token()`: JWTトークン生成
- `verify_token()`: トークン検証成功/失敗
- `authenticate_user()`: ユーザー認証成功/失敗
- `get_current_user()`: 現在ユーザー取得

#### 実装例
```python
class TestAuthHandler:
    def test_verify_password_success(self):
        password = "testpassword123"
        hashed = get_password_hash(password)
        assert verify_password(password, hashed) is True
    
    def test_create_access_token(self):
        data = {"sub": "testuser"}
        token = create_access_token(data, expires_delta=timedelta(minutes=30))
        assert isinstance(token, str) and len(token) > 0
```

### 2. OpenAI クライアント (openai_client.py) - 1関数

#### テスト対象
- `generate_minutes()`: 議事録生成成功/APIキー未設定/APIエラー

#### 実装例
```python
@pytest.mark.asyncio
async def test_generate_minutes_success(self, mocker, mock_openai_client):
    mocker.patch('app.modules.openai_client.client', mock_openai_client)
    result = await generate_minutes("テストトランスクリプト", user_id=1)
    assert result == "生成された議事録"
```

### 3. Graph クライアント (graph_client.py) - 4メソッド

#### テスト対象
- `GraphClient.__init__()`: 初期化成功/認証情報不足
- `get_user_meetings()`: 会議取得成功/クライアント未初期化
- `get_meeting_transcripts()`: トランスクリプト取得成功/エラー
- `get_transcript_content()`: 内容取得成功/エラー

### 4. データベースモジュール (database.py) - 2モデル

#### テスト対象
- `User`モデル: 作成、バリデーション、リレーションシップ
- `Minutes`モデル: 作成、外部キー、カスケード削除

#### 実装例
```python
def test_user_minutes_relationship(self, db_session):
    user = User(username="testuser", email="test@example.com", hashed_password="hash")
    db_session.add(user)
    db_session.commit()
    
    minutes = Minutes(user_id=user.id, title="会議", transcript="内容", generated_minutes="議事録")
    db_session.add(minutes)
    db_session.commit()
    
    assert len(user.minutes) == 1
    assert minutes.user == user
```

## スキーマ検証テスト設計

### 1. 認証スキーマ (auth.py) - 5スキーマ

#### テスト対象
- `UserCreate`: 有効データ/無効ユーザー名/無効メール/無効パスワード
- `UserLogin`: 有効データ/必須フィールド
- `Token`: トークン構造検証
- `TokenData`: オプショナルフィールド
- `UserResponse`: レスポンス構造検証

#### 実装例
```python
def test_user_create_invalid_username(self):
    invalid_cases = [
        {"username": "ab", "email": "test@example.com", "password": "password123"},  # 短すぎる
        {"username": "user@name", "email": "test@example.com", "password": "password123"},  # 無効文字
    ]
    for case in invalid_cases:
        with pytest.raises(ValidationError):
            UserCreate(**case)
```

### 2. 議事録スキーマ (minutes.py) - 3スキーマ

#### テスト対象
- `MinutesGenerate`: トランスクリプト長制限/タイトル省略可能
- `MinutesResponse`: 完全レスポンス構造
- `MinutesHistoryResponse`: 履歴レスポンス構造

### 3. Graph スキーマ (graph.py) - 6スキーマ

#### テスト対象
- `MeetingInfo`: 会議情報構造/オプショナルフィールド
- `MeetingListResponse`: 会議一覧構造
- `TranscriptInfo`: トランスクリプト情報構造
- `TranscriptListResponse`: トランスクリプト一覧構造
- `TranscriptContentResponse`: 内容レスポンス構造
- `GraphMinutesGenerate`: Graph議事録生成リクエスト

## エラーハンドリングテスト

### HTTP例外テスト
- 400 Bad Request: 無効データ、重複データ
- 401 Unauthorized: 未認証、無効トークン
- 404 Not Found: 存在しないリソース
- 422 Unprocessable Entity: バリデーションエラー
- 500 Internal Server Error: サーバーエラー
- 503 Service Unavailable: 外部API未設定

### 外部API障害テスト
- OpenAI API: 接続エラー、レート制限、無効レスポンス
- Microsoft Graph API: 認証エラー、権限不足、データ未発見

## テストユーティリティ

### フィクスチャ設計
```python
@pytest.fixture
def test_user(db_session):
    user = User(username="testuser", email="test@example.com", hashed_password=get_password_hash("password"))
    db_session.add(user)
    db_session.commit()
    return user

@pytest.fixture
def auth_headers(test_user):
    token = create_access_token(data={"sub": test_user.username})
    return {"Authorization": f"Bearer {token}"}

@pytest.fixture
def sample_minutes(db_session, test_user):
    minutes = Minutes(user_id=test_user.id, title="テスト会議", transcript="内容", generated_minutes="議事録")
    db_session.add(minutes)
    db_session.commit()
    return minutes
```

## テスト実行

### コマンド
```bash
# 全テスト実行
pytest

# カバレッジ付き実行
pytest --cov=app --cov-report=html

# 特定モジュールテスト
pytest tests/test_auth.py

# 並列実行
pytest -n auto
```

### カバレッジ目標
- **全体カバレッジ**: 90%以上
- **クリティカルパス**: 100%
- **エラーハンドリング**: 95%以上

## CI/CD統合

### GitHub Actions設定例
```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install poetry
          poetry install
      - name: Run tests
        run: poetry run pytest --cov=app --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v3
```

## テスト分類

### 単体テスト (Unit Tests)
- 個別関数・メソッドの独立テスト
- モック使用による外部依存排除
- 高速実行、高頻度実行

### 統合テスト (Integration Tests)
- コンポーネント間相互作用テスト
- データベース統合テスト
- 外部API統合テスト（実際のAPI使用）

### エンドツーエンドテスト (E2E Tests)
- 完全なユーザーワークフローテスト
- 認証→議事録生成→取得の一連の流れ
- Graph API→OpenAI→データベース保存の統合フロー

## パフォーマンステスト

### 負荷テスト観点
- 同時ユーザー数: 100ユーザー
- 議事録生成時間: 30秒以内
- データベースクエリ: 1秒以内
- メモリ使用量: 512MB以内

## セキュリティテスト

### テスト観点
- JWT トークン改ざん検出
- SQLインジェクション対策
- XSS対策（入力サニタイゼーション）
- 認証バイパス試行
- 権限昇格試行

## まとめ

本設計書に基づき、transcript_minutes_api2の全機能に対する包括的なテストスイートを構築することで、高品質で信頼性の高いAPIサービスを提供できます。

**テスト実装優先度**:
1. 認証機能（セキュリティクリティカル）
2. 議事録生成機能（コア機能）
3. Graph API統合（新機能）
4. エラーハンドリング（信頼性）
5. パフォーマンス（スケーラビリティ）

**総テストケース数**: 約150ケース  
**推定実装工数**: 40時間  
**メンテナンス工数**: 月2時間
